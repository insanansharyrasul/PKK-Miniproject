[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# F = pyflakes (unused imports, undefined names, redefinitions)
# E9 = syntax / indentation errors
select = ["F", "E9"]
ignore = ["F401"]   # allow explicit re-exports and side-effect imports
